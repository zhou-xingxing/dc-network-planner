# TODO
## 网络平面支持多级树状结构
- [x] 比如一个大的网络平面A(地址段掩码为22位)内可包含细分网络平面B和C(均为24位掩码地址段)
- [x] 最多支持3级嵌套关系

## 支持配置定时备份任务
- [x] 备份的是数据库文件
- [x] 支持配置是否开启备份任务，以及备份周期（每天、每周）
- [x] 允许自定义备份方式（备份到本地文件、备份到标准对象存储(用户需配置AK、SK、bucket)）

## 数据模型
- [x] Region的网络平面里加入vlanid（可选），网关位置（可选），网关IP（可选，公网私网自动推荐和弱校验网关IP）
- [x] 考虑Region内的网络平面增加作用域属性，因为可能存在一种网络平面在一个Region内有不同实例，例如external_om在管理AZ和网络AZ下都存在

## 认证登录及权限管理
- [x] 两种用户：administrator和user
- [x] 两种用户对所有数据（业务、配置数据）都有读权限
- [x] administrator可管理用户及权限分配，可管理全局配置，但不可管理Region内的业务数据
- [x] user拥有对所属Region的业务数据的全部管理权限

## 新功能
- [x] CIDR、VLAN重复性检测支持检测范围可配置化（是否跨Region检测）
- [x] 数据库备份文件恢复脚本
- [x] IP查找的逻辑，如果涉及多个父子平面能不能全查出来
- [x] 右上角用户名和角色区分的不够明显，加个个人主页
- [x] 系统日志配置，requestid调用链
- [x] 创建 Region 子网络平面实例时，根据实际父平面 CIDR 和目标掩码位数自动推荐地址最低的可用网段；推荐不实际写库，提交时重新校验 CIDR 冲突
- [ ] CIDR树状结构检查时会检查Scope吗
- [ ] 支持MCP？例如AI自动填写Excel表，查网段信息

## 交换机布线管理模块

- [x] 共通（已实现资源）：交换机业务类型由 administrator 管理，Region 内机柜和交换机数据由获得该 Region 授权的 user 管理
- [ ] 共通（待布线功能）：Region 内布线数据由获得该 Region 授权的 user 管理
- [x] 共通（已实现资源）：机柜、交换机业务类型、交换机组、交换机和端口的数据变更由 Service 显式记录审计日志，并将数据库删除限制转换为清晰的中文业务错误
- [ ] 共通（待布线功能）：布线批次和线缆条目的数据变更记录审计日志，并将数据库删除限制转换为清晰的中文业务错误

### 机柜管理
- [x] 按同机房、同机柜列聚合并折叠展示 Region 内机柜，展开时分页加载明细；通过统一添加页面生成并创建一个或多个机柜
- [x] 结构化保存机房名、机柜列和机柜编号，由后端生成最终名称并通过数据库约束保证一致
- [x] 校验机柜位置、名称、总 U 数及删除时的交换机和线缆引用关系

### 交换机管理
- [x] 管理交换机业务类型、交换机组、交换机基本信息及机柜上架位置；通过统一入口原子创建交换机组及完整成员
- [x] 管理交换机端口，组合新增交换机组时按范围生成成员端口，后续仍支持批量补充端口并查看空闲或已占用状态
- [x] 校验交换机上架范围不超过机柜总 U 数，且不与同机柜的其他交换机或服务器侧位置重叠
- [x] 校验机柜与交换机组属于同一 Region；pair 组必须包含端口速率一致的 a、b 成员，single 组必须包含 single 成员，未完成成员配置的组不得用于布线规划
- [x] 处理交换机组、交换机和端口被引用时的删除限制
- [ ] 交换机任一端口已有布线时，禁止解除分组、转移到其他交换机组或变更成员角色

### 布线规划
- [x] 支持在同一布线规划中录入一台或多台服务器；每台服务器以机柜、起始 U 位和设备高度标识服务器侧位置
- [x] 一台服务器可配置一个或多个目标交换机组；每个目标组设置连线根数，并为每根线录入唯一的服务器端口名
- [x] 页面按连线根数动态生成服务器端口输入项，并将明细列表转换为不含页面临时状态的规范化需求
- [x] 输入页校验 single 组允许任意正整数根连线、pair 组只允许正偶数根并平均分配给 a、b 成员；预览和确认服务后续重新强校验
- [ ] 按交换机组成员分别校验所需端口数和空闲端口数，不仅校验组内总端口数；为每根线自动分配可用交换机端口、线序号和线签
- [ ] 将每个服务器端口展开为一条不落库的布线预览记录，展示服务器位置和端口、交换机组、成员角色、交换机及端口、线序号和线签
- [x] 输入页只展示当前 Region 的机柜和交换机组，并禁用成员配置未完整的目标组；预览和确认服务后续重新强校验 Region 归属与成员完整性
- [x] 输入页以“服务器侧机柜 + 起始 U 位 + 服务器端口名”校验当前需求及已有线缆的端点重复；端口名去除首尾空格，只允许小写字母、数字和用于分隔非空片段的单个中划线
- [x] 输入页查询机柜占用快照，校验服务器位置未越界，不与当前输入、已有服务器位置或交换机上架位置重叠；同一隐式服务器位置的设备高度必须一致
- [ ] 保证同一交换机连接到同一服务器侧机柜的线序号跨布线批次唯一，并可按已有最大序号自动分配下一个值

### 布线批次与线缆台账
- [ ] 用户确认布线预览后，重新执行完整业务校验，并在同一事务内创建布线批次和全部线缆条目；任一条失败时整批回滚
- [ ] 确认落库时重新校验交换机组配置和端口占用状态，防止预览后资源发生变化
- [ ] 支持查看布线批次列表和批次详情，并按批次、机柜、U 位占用范围、交换机、端口和线签查询线缆
- [ ] 支持导出指定批次或当前筛选结果的布线表，导出字段和顺序与线缆台账保持一致
- [ ] 撤销线缆时先记录包含服务器侧端点、交换机及端口和线签的完整变更日志，再删除线缆条目并释放交换机端口和线签
- [ ] 布线批次仍有线缆条目时禁止删除；清空线缆条目后才允许删除空批次

## OpenAPI
- [x] 获取Token接口
- [x] 管理员手动撤销Token
- [ ] scope设计
- [x] 查询IP接口
- [ ] 批量导入接口


## 文档
- [ ] 总结项目亮点

## 后续可扩展方向
- 前端如果有逻辑代码，也需要加上测试用例
- **PostgreSQL 迁移**：支持空间索引优化 CIDR 查询
- **审计增强**：变更日志支持 Entity 关联查询、差异对比
- **批量操作**：支持批量编辑/删除 Region 网络平面
- **可视化**：Region 网络平面可视化拓扑图
- **导入模板增强**：自动识别 Excel 列名、支持多 sheet


## review
- [x] auth
- [x] backup
- [ ] network_plane_type
- [ ] region
- [ ] region_plane
- [ ] Dockerfile & docker-compose.yml

## 优化
- [ ] 代码架构是否符合分层设计
- [ ] 统一审查 API 请求/响应 Schema：确认每个接口都在 `app/schemas/` 中使用明确的 Pydantic Schema，并与 `app/models/` 下的 SQLAlchemy 数据库模型保持职责分离；核查字段是否为接口实际必需，提取可复用的公共 Schema，减少无约束的 `dict[str, Any]`，重构时保持现有接口契约兼容
- [x] Region 网络平面 CIDR 必须使用网段的网络地址，例如拒绝将 `10.0.0.1/30` 作为 CIDR 并提示使用 `10.0.0.0/30`；IPv4 `/32` 和 IPv6 `/128` 允许使用地址本身
- [ ] 同一次请求中尽量减少数据库查询次数
- [ ] 数据库索引优化
- [ ] 方法该抛异常的要抛异常，docstring也要说明
- [x] 确认所有级联删除行为是否合理，级联删除的实现方式
- [x] 测试用例加注释
- [x] user和auth的边界
- [x] Excel表头/字段优化
- [x] 导入导出功能的权限、导入内容如果已存在是覆盖还是跳过
